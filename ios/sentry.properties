# Sentry configuration for iOS
# Disable automatic uploads for now - you can enable this later when you have Sentry properly configured
upload.disable=true

# When you're ready to enable Sentry uploads, uncomment and configure these:
# org=your-sentry-org-slug
# project=your-sentry-project-slug
# auth.token=your-sentry-auth-token
